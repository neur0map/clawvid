import { describe, it, expect } from 'vitest';
import { workflowSchema } from '../../src/schemas/workflow.js';

describe('Schema: workflow', () => {
  it.todo('should validate valid workflow JSON');
  it.todo('should reject workflow without scenes');
  it.todo('should reject workflow without title');
  it.todo('should validate scene types');
});
