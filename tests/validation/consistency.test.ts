import { describe, it, expect } from 'vitest';

describe('Validation: consistency', () => {
  it.todo('should detect brightness drift between scenes');
  it.todo('should detect saturation drift between scenes');
  it.todo('should pass consistent scene set');
});
