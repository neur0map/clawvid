import { describe, it, expect } from 'vitest';
import { planScenes } from '../../src/core/scene-planner.js';

describe('Core: scene-planner', () => {
  it.todo('should return correct number of scenes for 60s video');
  it.todo('should not exceed max video clips');
  it.todo('should assign correct scene types per template');
  it.todo('should sum scene durations to total duration');
});
