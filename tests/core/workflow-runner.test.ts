import { describe, it, expect } from 'vitest';

describe('Core: workflow-runner', () => {
  it.todo('should execute all workflow steps in order');
  it.todo('should skip cached steps');
  it.todo('should track costs per step');
  it.todo('should handle step failures gracefully');
});
